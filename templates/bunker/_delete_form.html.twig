<form method="post" action="{{ path('app_bunker_delete', {'id': bunker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bunker.id) }}">
    <button class="btn">Delete</button>
</form>
