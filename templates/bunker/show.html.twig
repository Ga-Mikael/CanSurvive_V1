{% extends 'base.html.twig' %}

{% block title %}Bunker{% endblock %}

{% block body %}
    <h1>Bunker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bunker.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bunker.name }}</td>
            </tr>
            <tr>
                <th>StockCapacity</th>
                <td>{{ bunker.stockCapacity }}</td>
            </tr>
            <tr>
                <th>ResidentCapacity</th>
                <td>{{ bunker.residentCapacity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bunker_index') }}">back to list</a>

    <a href="{{ path('app_bunker_edit', {'id': bunker.id}) }}">edit</a>

    {{ include('bunker/_delete_form.html.twig') }}
{% endblock %}
