<form method="post" action="{{ path('app_resident_delete', {'id': resident.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resident.id) }}">
    <button class="mt-3 btn btn-primary text-secondary rounded-0 opacity-50">{{'décédé'|upper}}</button>
</form>
