<form method="post" action="{{ path('app_can_delete', {'id': can.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ can.id) }}">
    <button class="mt-3 btn btn-primary text-secondary rounded-0 opacity-50">Supprimer</button>
</form>
